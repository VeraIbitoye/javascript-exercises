// a function that takes a string as input and capitalizes the first letter of each word in the string
function capitalizedWord() {
    let word = "my name is vera";
    let capitalizedWord = word.toUpperCase();
    console.log(capitalizedWord);
}
capitalizedWord() 


